<?xml version="1.0" encoding="utf-8"?>
<ScrollView xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:background="@color/color_surface">

    <LinearLayout android:layout_width="match_parent" android:layout_height="wrap_content"
        android:orientation="vertical" android:padding="16dp">

        <!-- Language selector -->
        <TextView android:layout_width="wrap_content" android:layout_height="wrap_content"
            android:text="@string/language"
            android:textColor="@color/color_primary"
            android:textSize="10sp" android:textStyle="bold"
            android:layout_marginBottom="4dp"/>

        <LinearLayout android:id="@+id/language_selector"
            android:layout_width="match_parent" android:layout_height="44dp"
            android:orientation="horizontal" android:gravity="center_vertical"
            android:background="@drawable/user_card_bg"
            android:paddingStart="12dp" android:paddingEnd="12dp"
            android:layout_marginBottom="12dp"
            android:focusable="true" android:clickable="true">
            <TextView android:layout_width="0dp" android:layout_height="wrap_content"
                android:layout_weight="1"
                android:text="@string/select_language"
                android:textColor="@color/color_text" android:textSize="12sp"/>
            <TextView android:id="@+id/text_current_language"
                android:layout_width="wrap_content" android:layout_height="wrap_content"
                android:textColor="@color/color_text_secondary" android:textSize="11sp"/>
        </LinearLayout>

        <!-- Switches -->
        <androidx.appcompat.widget.SwitchCompat android:id="@+id/switch_sort_channel"
            style="@style/SettingSwitch" android:text="@string/sort_channel"/>
        <androidx.appcompat.widget.SwitchCompat android:id="@+id/switch_sort_category"
            style="@style/SettingSwitch" android:text="@string/sort_category"/>
        <androidx.appcompat.widget.SwitchCompat android:id="@+id/switch_sort_favorite"
            style="@style/SettingSwitch" android:text="@string/sort_favorite"/>
        <androidx.appcompat.widget.SwitchCompat android:id="@+id/switch_play_last_watched"
            style="@style/SettingSwitch" android:text="@string/play_last_watched"/>
        <androidx.appcompat.widget.SwitchCompat android:id="@+id/switch_launch_at_boot"
            style="@style/SettingSwitch" android:text="@string/launch_at_boot"/>
        <androidx.appcompat.widget.SwitchCompat android:id="@+id/switch_optimize_prebuffer"
            style="@style/SettingSwitch" android:text="@string/optimize_prebuffer"/>
        <androidx.appcompat.widget.SwitchCompat android:id="@+id/switch_reverse_navigation"
            style="@style/SettingSwitch" android:text="@string/reverse_player_navigation"/>

    </LinearLayout>
</ScrollView>