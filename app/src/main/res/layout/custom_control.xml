<?xml version="1.0" encoding="utf-8"?>
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:id="@+id/custom_control"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:orientation="vertical"
    android:gravity="bottom">

    <!-- Control bar -->
    <LinearLayout
        android:id="@+id/layout_control"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:orientation="vertical"
        android:background="@color/control_background"
        android:paddingStart="12dp"
        android:paddingEnd="12dp"
        android:paddingTop="8dp"
        android:paddingBottom="12dp">

        <!-- Seekbar row (VOD only) -->
        <LinearLayout android:id="@+id/layout_seekbar"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:orientation="horizontal"
            android:gravity="center_vertical"
            android:paddingBottom="8dp"
            android:visibility="gone">
            <com.google.android.exoplayer2.ui.DefaultTimeBar
                android:id="@id/exo_progress"
                android:layout_width="0dp"
                android:layout_height="26dp"
                android:layout_weight="1"/>
        </LinearLayout>

        <View android:id="@+id/spacer_control"
            android:layout_width="match_parent"
            android:layout_height="0.5dp"
            android:background="@color/color_divider"
            android:layout_marginBottom="8dp"
            android:visibility="gone"/>

        <!-- Buttons row -->
        <LinearLayout
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:orientation="horizontal"
            android:gravity="center_vertical">

            <ImageButton android:id="@+id/button_previous"
                style="@style/PlayerControlButton"
                android:src="@drawable/ic_player_previous"/>

            <ImageButton android:id="@+id/button_rewind"
                style="@style/PlayerControlButton"
                android:src="@drawable/ic_player_rewind"
                android:layout_marginStart="4dp"
                android:visibility="gone"/>

            <!-- Play/Pause (ExoPlayer managed) -->
            <ImageButton android:id="@id/exo_play"
                style="@style/PlayerControlButton"
                android:src="@drawable/ic_player_play"
                android:layout_marginStart="4dp"/>
            <ImageButton android:id="@id/exo_pause"
                style="@style/PlayerControlButton"
                android:src="@drawable/ic_player_pause"
                android:layout_marginStart="4dp"/>

            <ImageButton android:id="@+id/button_forward"
                style="@style/PlayerControlButton"
                android:src="@drawable/ic_player_forward"
                android:layout_marginStart="4dp"
                android:visibility="gone"/>

            <ImageButton android:id="@+id/button_next"
                style="@style/PlayerControlButton"
                android:src="@drawable/ic_player_next"
                android:layout_marginStart="4dp"/>

            <!-- Spacer -->
            <View android:layout_width="0dp" android:layout_height="1dp"
                android:layout_weight="1"/>

            <!-- Screen mode -->
            <ImageButton android:id="@+id/screen_mode"
                style="@style/PlayerControlButton"
                android:src="@drawable/ic_aspect_ratio"/>

            <!-- Track selection -->
            <ImageButton android:id="@+id/track_selection"
                style="@style/PlayerControlButton"
                android:src="@drawable/ic_track_selector"
                android:layout_marginStart="4dp"/>

            <!-- Lock -->
            <ImageButton android:id="@+id/button_lock"
                style="@style/PlayerControlButton"
                android:src="@drawable/ic_lock_open"
                android:layout_marginStart="4dp"/>

            <!-- Exit -->
            <ImageButton android:id="@+id/button_exit"
                style="@style/PlayerControlButton"
                android:src="@drawable/ic_exit"
                android:layout_marginStart="4dp"/>

        </LinearLayout>
    </LinearLayout>
</LinearLayout>