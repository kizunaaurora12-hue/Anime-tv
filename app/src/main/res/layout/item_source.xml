<?xml version="1.0" encoding="utf-8"?>
<layout xmlns:android="http://schemas.android.com/apk/res/android">
    <data>
        <variable name="sourcesAdapter" type="com.miyuki.tv.adapter.SourcesAdapter"/>
    </data>
    <LinearLayout
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:orientation="horizontal"
        android:gravity="center_vertical"
        android:paddingStart="4dp"
        android:paddingEnd="4dp"
        android:paddingTop="6dp"
        android:paddingBottom="6dp">

        <CheckBox android:id="@+id/source_active"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:buttonTint="@color/color_primary"/>

        <TextView android:id="@+id/source_path"
            android:layout_width="0dp"
            android:layout_height="wrap_content"
            android:layout_weight="1"
            android:textColor="@color/color_text"
            android:textSize="11sp"
            android:maxLines="2"
            android:ellipsize="end"
            android:paddingStart="4dp"
            android:paddingEnd="4dp"/>

        <ImageButton android:id="@+id/source_copy"
            android:layout_width="32dp"
            android:layout_height="32dp"
            android:background="@drawable/button_control_background"
            android:src="@drawable/ic_pick_file"
            android:padding="6dp"
            android:layout_marginEnd="4dp"/>

        <ImageButton android:id="@+id/source_remove"
            android:layout_width="32dp"
            android:layout_height="32dp"
            android:background="@drawable/button_control_background"
            android:src="@drawable/ic_remove"
            android:padding="6dp"/>
    </LinearLayout>
</layout>